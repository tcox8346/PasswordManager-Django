This project requires the instillation of python, and its pip3 modules pipenv. The other neccessary modules in the active virtual enviorement are present in the pip file

pip install:

sendgrid


















SETUP 
SendGrid webapi setup
-- Update your development environment with your SENDGRID_API_KEY. Run the following in your shell:
echo "export SENDGRID_API_KEY='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'" > sendgrid.env
echo "sendgrid.env" >> .gitignore
source ./sendgrid.env

Code that sends emails 
# using SendGrid's Python Library
# https://github.com/sendgrid/sendgrid-python
import os
from sendgrid import SendGridAPIClient
from sendgrid.helpers.mail import Mail

message = Mail(
    from_email='from_email@example.com',
    to_emails='to@example.com',
    subject='Sending with Twilio SendGrid is Fun',
    html_content='<strong>and easy to do anywhere, even with Python</strong>')
try:
    sg = SendGridAPIClient(os.environ.get('SENDGRID_API_KEY'))
    response = sg.send(message)
    print(response.status_code)
    print(response.body)
    print(response.headers)
except Exception as e:
    print(e.message)

Encryption
https://pypi.org/project/django-searchable-encrypted-fields/